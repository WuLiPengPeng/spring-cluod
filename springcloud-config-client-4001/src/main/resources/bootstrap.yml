# bootstrap.yml,系统级别的配置
spring:
  cloud:
    config:
      name: config-client #需要从git上获取的资源，不需要后缀
      # config-server的IP地址
      uri: http://localhost:3001
      profile: dev
      label: master #从哪个分支获取